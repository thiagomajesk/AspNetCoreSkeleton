@model Project.App.Features.Post.Commands.UpdatePostCommand

@{ ViewBag.Action = Url.Action("Edit"); }

<div class="row">
    <div class="col-md-8 m-auto">
        <div class="card">
            <div class="card-body">
                <partial name="_Form" />
            </div>
        </div>
    </div>
</div>